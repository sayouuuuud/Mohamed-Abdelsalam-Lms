# ─── Database ──────────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres:Mohamed2006abdeelsalam@169.58.19.247:5432/postgres
# ─── Cloudflare R2 ────────────────────────────────────────────────
R2_ACCOUNT_ID=abc123
R2_ACCESS_KEY_ID=key_id
R2_SECRET_ACCESS_KEY=secret_key
R2_BUCKET=lms-videos
R2_ENDPOINT=https://abc123.r2.cloudflarestorage.com

# ─── Worker identity (اختياري، للـ claim_video_job) ──────────────
WORKER_ID=worker-1

# ─── وضع التشغيل ─────────────────────────────────────────────────
# "poll"  = polling دائم (always-on)
# "wake"  = HTTP server ينتظر إشارة الصحيان (scale-to-zero)
WORKER_MODE=wake

# ─── لو WORKER_MODE=wake ─────────────────────────────────────────
WAKE_PORT=3001
WORKER_WAKE_SECRET=some_secret_here

# ─── Polling interval (لو WORKER_MODE=poll) ──────────────────────
POLL_INTERVAL_MS=10000

# ─── مسار FFmpeg (اختياري، الافتراضي: ffmpeg في PATH) ────────────
# FFMPEG_PATH=/usr/bin/ffmpeg
